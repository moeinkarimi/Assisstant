<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mytechcorp.ir.assisstant">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GeneralInfoActivity"></activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".DescriptionActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CodeActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".EnterActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TableActivity" />
        <activity android:name=".ReadingActivity" />
        <activity android:name=".ShowPersonActivity" />
        <activity android:name=".AnswerDialogActivity" />
        <activity android:name=".AddPersonActivity" />
        <activity android:name=".GCodeActivity" />
        <activity android:name=".ShowScoresActivity" />
        <activity android:name=".MysteryActivity" />
        <activity android:name=".ReadingQuestionActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".FinalActivity" />
    </application>

</manifest>